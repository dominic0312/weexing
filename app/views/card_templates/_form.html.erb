<%= form_for(@card_template) do |f| %>
  <% if @card_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_template.errors.count, "error") %> prohibited this card_template from being saved:</h2>

      <ul>
      <% @card_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :card_name %><br />
    <%= f.text_field :card_name %>
  </div>
  <div class="field">
    <%= f.label :card_image_url %><br />
    <%= f.text_field :card_image_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
